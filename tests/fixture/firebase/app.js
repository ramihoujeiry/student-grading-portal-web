// Stub for 'firebase/app' — only the symbols store.js imports.
// In the fixture FIREBASE_READY is false, so initializeApp is never reached,
// but the import must resolve for the module to load in a plain browser.
export function initializeApp() { return {}; }
export function getApp() { return {}; }
