{
  "name": "student-grading-portal-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Student Grading Portal PWA — Vite-bundled Vue3 SPA (code-split, Firebase ESM SDK, lazy RAG).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "firebase": "^11.10.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
